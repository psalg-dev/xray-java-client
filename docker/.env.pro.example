# Copy this to docker/.env and fill in your values before running:
#   docker compose --profile pro --env-file docker/.env up -d

ARTIFACTORY_IMAGE=releases-docker.jfrog.io/jfrog/artifactory-pro:latest
ARTIFACTORY_LICENSE_PATH=/path/to/your/artifactory.lic
POSTGRES_PASSWORD=password
